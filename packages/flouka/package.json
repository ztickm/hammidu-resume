{
  "name": "flouka",
  "version": "1.0.0",
  "type": "module",
  "description": "PDF renderer and web interface for JSON Resume using Puppeteer",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "web": "bun run web-server.ts"
  },
  "dependencies": {
    "xebec": "workspace:*",
    "puppeteer": "^23.0.0",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
