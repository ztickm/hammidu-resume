{
  "name": "pdfts-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --cwd packages/scpdf build",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
