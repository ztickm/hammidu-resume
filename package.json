{
  "name": "hammidu-resume",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --cwd packages/xebec build",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
