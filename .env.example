# Required — Anthropic API key for Claude
ANTHROPIC_API_KEY=sk-ant-your-key-here

DEEPSEEK_API_KEY=sk-your_deepseek_key_here

# Optional — SQLite path for LangGraph checkpointing (persists master resume across sessions)
# AGENT_DB_PATH=agent.sqlite

# Optional — Directory where generated PDFs are written
# AGENT_PDF_DIR=./output